# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Fixed multiple character scenarios on character selection where the characters spawned, they all used the scenarios from character #1 than using their own that are listed on the configuration file.
2. Fixed a function call that it was `RequestImap` than `RemoveImap` on `183712523`.
3. Fixed an issue where `RemoveImaps()` function was never executed when it should be after creating a new character.
4. Fixed an issue on command `eyewear` that was executing the wrong outfit (poncho) instead of eyewear.
5. Fixed on loading character body features, there was a `value` that was actually returning `null` on `SetCharExpression`.

## Modified Files:

- client/main.lua
- client/nui.lua
- client/functions.lua
- client/commands.lua
- client/clothing_component_functions.lua
- fxmanifest.lua

## Notes

- `THERE ARE NO NOTES.`
